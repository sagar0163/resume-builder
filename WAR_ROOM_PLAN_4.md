# WAR_ROOM_PLAN_4

- [ ] Update CORS policy in `backend/app.py` to allow specific domains instead of all.
- [ ] Use environment variables to set allowed origins with sensible defaults (e.g., `http://localhost:3000`).
- [ ] Commit changes.
