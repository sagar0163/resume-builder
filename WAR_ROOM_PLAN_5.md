# Plan for Issue #5

- [x] Initialize a Vue 3 project using Vite in a temporary directory and move it to `frontend`.
- [x] Install TailwindCSS and configure it in the Vite project.
- [ ] Create proper state management (Pinia or Composition API composable) for the resume data.
- [ ] Break down `index.html` into Single File Components (e.g., `App.vue`, `ResumeList.vue`, `ResumeForm.vue`, `ResumePreview.vue`).
- [ ] Integrate API calls into the new state management / composables.
- [ ] Verify functionality (build works).
- [ ] Remove old `index.html` and `ai-features.js` if they are superseded (Wait, I should check what `ai-features.js` does before removing it. I'll port it if necessary).
